# 经验教训 (Lessons Learned)

> 项目级防错规则；当前方向见 `KANBAN.md`。

压缩规则库——每条2-3行，保留可执行规则，省略叙事背景。

## 0. 退役被忽略的本地记录前先做可恢复副本
**规则**: 删除 `.gitignore` 命中的本地任务文件前，先读取并用 `apply_patch` 迁入已跟踪 archive，再删除原路径；Git 历史不能恢复从未跟踪的版本。
**反例**: 先删 ignored `tasks/todo.md` → 只能从已跟踪历史和其他状态文档重建概要，不能证明逐字保留。

## 1. OAuth签名基准URL必须强制HTTP
**规则**: `fanfou.com/oauth` 和 `api.fanfou.com` 的签名基准串必须强制替换为 `http://`（后端只识别http签名）
**反例**: 保留https → `401 Invalid signature`

## 2. page.js禁止require后台模块
**规则**: page.js中禁止 `requireModules(['messaging'])` 等后台底层接口；只能用已IPC穿透暴露的中间件（如 `fanfouOAuth`）
**反例**: 顶层模块找不到 → 阻断所有features装载循环 → 所有页面组件消失

## 3. WebAuthFlow沙箱：OAuth后放弃Cookie
**规则**: 一旦使用 `launchWebAuthFlow` 授权，完全转向OAuth API；禁止混合Cookie+Token
**反例**: 混合使用 → 扩展fetch无法携带主域Cookie → 幽灵行为

## 4. MV3 Bridge跨层通信必须防御性try-catch
**规则**: 所有 `await bridge.postMessageToBackground()` 必须包裹try-catch；错误必须回传前台释放所有挂起的Deferred；`hasChecked` 等阻拦标记必须在成功+失败路径末端同时释放
**反例**: SW休眠后port断开 → 未捕获异常 → 前台永久死锁

## 5. 可见性功能必须体感验证
**规则**: 提交前必须3组真实样本手测（如短/中/长转发链）；阈值默认偏易触发；同类功能并存时文案需明确边界
**反例**: 只跑构建验证 → 阈值偏高 → 用户感知"没生效"

## 6. 优先排序不可被全量shuffle覆盖
**规则**: "固定优先+局部随机"必须拆分：优先段不动，仅对普通段shuffle
**反例**: 先排序再对全量shuffle → 优先设置对用户无感

## 7. 装饰型功能默认关闭
**规则**: 视觉占比高、风格化强的功能用 `defaultValue: false`；文档注明"默认关闭，可手动开启"
**反例**: 默认开启 → 改变第一观感 → 用户感知干扰

## 8. SW冷启动IPC必须设初始化屏障
**规则**: Background `onMessage` 内部维护 `__SF_BACKGROUND_READY__` 完成屏障；挂起冷启动期间到达的消息，等初始化完成后统一分发
**反例**: 裸发IPC → `await asyncInitialize()` 期间消息到达 → handler未注册 → settings白屏

## 9. 生产构建必须禁用Macro相关缓存
**规则**: 含 `import-all.macro` 的生产构建必须禁用 `cache-loader`/`babel-loader` AST缓存；新增/删除 `src/features/` 子目录后必须强制全量重建
**反例**: 缓存命中旧AST → 新增feature不被装载 → `optionDefs` 字段缺失 → 白屏

## 10. 消除玩法：1类型=1可视头像
**规则**: 以头像为识别元素的消除玩法，1类型必须绑定1个用户头像；不做隐式分桶映射
**反例**: 多用户映射同类型 → "头像不同但可消" → 玩家认知冲突

## 11. 参考玩法先还原3个最小形态特征
**规则**: 实现"像某某"需求时，先还原3个最小形态（层叠感/下层露出/槽位空间组织），再优化细节；验收标准必须含视觉结构
**反例**: 只做规则迁移 → 逻辑像但体验不像

## 12. 参考玩法的UI视觉复杂度要匹配
**规则**: 槽位与牌面同时具备层次（阴影/圆角/过渡动效）；点击/入槽/消除等关键路径必须至少有1个方向性动效
**反例**: 简陋矩形槽位 → 即使逻辑正确 → 用户判定"不够像"

## 13. 操作区优先边缘落位，不遮内容
**规则**: 操作条/槽位落在边缘（左下/右下）并预留安全边距；提供基础操作按钮（反悔/打乱/重开）
**反例**: 中下区域放槽 → 压住时间线 → 用户判定"不能用"

## 14. 圆头像场景必须用固定1:1容器
**规则**: 圆头像的槽位和牌槽必须使用固定圆形尺寸或严格1:1比例；不可被布局挤压变形
**反例**: 宽高不固定 → 头像被压扁 → 感知不精致

## 15. 堆叠用阴影表达深度，不用多层半透明
**规则**: 用"清晰顶牌+厚度阴影/偏移"表达堆叠层次；禁止叠多层半透明头像做预览；确需显示下层时限制透明层数，保证顶牌边界清晰可辨
**反例**: 多层半透明叠加 → 画面雾化发灰 → 用户感知"乱"和"脏"

## 16. 表单操作条验收必须看基线，不只看左右位置
**规则**: 涉及输入框下方操作条（图标/计数器/按钮）的调整，验收必须同时检查左右落位和垂直基线；至少对齐图标可点击区、计数器 pill、按钮高度三者
**反例**: 只确认"已经贴左" → 上传图标/字数提示/发送按钮仍上下不齐 → 用户继续感知为没修完

## 17. 视觉基线不能用容器居中替代
**规则**: 若目标是让图标/数字/按钮文字“看起来在同一高度”，优先调字高、line-height、padding 和 1-2px 光学校正，不要只用 `align-items: center`
**反例**: 盒子都居中了，但 `140` 和“发送”字面仍一高一低 → 用户会继续判定没对齐

## 18. 部分样本成功时必须继续核对输入数据差异
**规则**: 当同一功能出现“有些样本成功、有些样本失败”时，不要过早收敛成单一路径 bug；继续比较进入系统前后的 `type`、`size`、编码和来源链路差异
**反例**: 只看到一次修复后有样本成功就宣告根因已定 → 忽略剪贴板重编码/体积膨胀这类输入侧差异

## 19. 时间线/分页类 bug 必须做真实登录态终验
**规则**: 涉及 `#stream`、分页按钮、懒加载或饭否原生 JS 报错的修复，关闭任务前必须在真实登录态页面点一遍关键路径，并记录点击前后末尾节点/控制台结果；Jest 结构测试只能算前置校验，不能替代终验
**反例**: 只凭 DOM 结构推断和本地单测就收工 → 用户侧仍能复现“更多”重复旧消息

## 20. 提交 upstream PR 前先校验分支基线
**规则**: 准备向 `upstream/main` 提 PR 时，先跑 `git rev-list --left-right --count upstream/main...<branch>`；若不是“仅 ahead 少量提交”，不要直接推 PR，先从 `upstream/main` 新切干净分支或重整分支历史
**反例**: 直接把 fork 内整理分支当成 PR 分支使用 → GitHub 显示几十个无关历史提交和冲突，PR 无法干净审阅

## 21. 多 worktree 构建前先核对目标目录
**规则**: 用户提到具体 worktree 或功能分支时，构建前先核对“当前执行目录”“当前分支”“浏览器实际加载的扩展目录”三者是否一致；不要默认在主 worktree 执行 `npm run build`
**反例**: 功能改动在 `feat/avatar-wallpaper` worktree，但构建发生在 `2026.2` 主目录 → `dist` 成功生成却仍是旧版本

## 22. 扩展跨 worktree 验收必须考虑 storage 隔离
**规则**: 对比两个 unpacked worktree 的扩展行为时，先确认 `manifest.json` 是否带固定 `key`；若没有，默认它们使用不同扩展 ID 和不同 `chrome.storage`，涉及缓存类功能时要做迁移或强制刷新
**反例**: 只合并源码不处理缓存 schema → 分支里是新抓取的全量头像，主分支继续沿用旧缓存 → 用户看到“同代码但头像数不同”

## 23. 用户说“要显示全部”时先改布局目标，不要只追代码一致
**规则**: 用户反馈“还是不对，但我现在只要全部显示出来”时，要立即把问题从分支对齐切回真实界面目标；优先检查容量裁剪、列数取整和最后一排分配，而不是继续执着于 worktree 差异
**反例**: 代码已对齐却还围绕缓存/分支兜圈子 → 用户真实要的是“多显示头像、补齐最后一排”，问题迟迟不落到布局算法上

## 24. 同一功能跨扩展实例不一致时先查数据源，不先怪页面渲染
**规则**: 如果 worktree 和主分支同页表现不一致，而源码差异已经收敛，要优先核对它们是否依赖了不同的 OAuth、session 缓存或站点接口；必要时给功能补一个不依赖扩展实例状态的页面级数据源兜底
**反例**: 一直改布局和缓存 schema，但主分支其实只是没拿到和 worktree 同一份好友列表 → 用户继续看到“worktree 行，主分支不行”

## 25. 分发方案先确认 listing 所有权和 extension ID
**规则**: 讨论 Chrome 插件发布、更新、用户迁移前，先确认原 Chrome Web Store listing 是否可管理、当前 `manifest.json` 是否有固定 `key`、开发者模式加载目录是否会改变 extension ID；凡是依赖 `chrome.storage.local` 的本地记录，面向用户更新前先设计 export/import 或稳定 ID 方案
**反例**: 只按“提交商店审核”或“换文件夹加载新版”推进 → 原 listing 不归本 fork 管理，且新文件夹可能生成新扩展 ID，头像消消乐本地战绩在用户侧看起来丢失

## 26. 改原生站点样式前先抓真实 computed style
**规则**: 覆盖饭否原生元素的布局前，先用 agent-browser 登录后 `getComputedStyle` 确认目标元素的 display/float/width，再写覆盖规则；浮动（float）会让块级子元素收缩打包并排，是「明明写了 display:block 还挤一行」的常见真凶。另：agent-browser 默认视口过窄会触发饭否响应式隐藏侧栏（元素 0 宽），先 `set viewport 1440 900`
**反例**: unify-sidebar-panels 列表模式按「.alist li 是浮动瓦片」的语义猜测写 CSS → 真凶其实是 `.alist a { float:left; width:48px }`，用户实测截图打回，两轮才修好

## 27. 仓库维护权不等于官方身份或商店条目控制权
**规则**: 原太空饭否 Chrome Web Store 条目已因 Manifest 未更新被强制下架；仓库维护者接受代码 PR，不代表其是官方主体或拥有原商店条目的发布权限。讨论分发前，必须分别核对仓库维护权、官方身份和 Web Store listing 控制权，不能从其中一项推断另外两项
**反例**: 因为仓库接受了 Manifest V3 PR，就假设官方会重新上架或可以沿用原条目 → 对现实分发路径和既有用户迁移作出错误判断

## 28. 临时验收界面也要使用用户语言
**规则**: 需要用户点击的临时探针、验收页和调试向导，按钮必须使用中文动作名称，并在按钮旁加一句说明；函数名和英文状态只保留在代码或日志中
**反例**: 页面直接展示 `pickDirectory()`、`queryPerm()` 等函数名 → 用户能完成操作，但必须猜每个选项的含义

## 29. 给设置页加纯 children 面板必须同步改 getTabDefs 的校验循环
**规则**: 在 `getTabDefs.js` 新增只有 `title` + `children`、没有 `options` 的 section 时，必须在校验循环里加 `if (!section.options) continue`；渲染侧 `App.js` 早有 `options = []` 兜底，容易误以为整条链路都安全
**反例**: 「个人归档」section 无 `options` → `b.push(...undefined)` 抛 TypeError → `App.init()` 无 catch → `isReady` 永远 false → 设置页整页白屏，连导航栏都不渲染

## 30. 外部资源域名必须从真实数据统计，不能从站点主域推断
**规则**: 判断「要不要新增 host 权限」前，必须对真实备份/抓取结果统计一次域名分布（`new URL(u).host` 计数），不能因为站点是 `fanfou.com` 就假设资源也在 `*.fanfou.com`；代码里的域名白名单必须有单测断言与 `manifest.json` 的 `host_permissions` 一致，防止改一边忘另一边
**反例**: spec 4.7 断言「`https://*.fanfou.com/` 已覆盖图片域名，无需新增权限」→ 实测图片在 `s3-img.meituan.net`、头像在 `s3.meituan.net`，按原断言实现会被 CORS 全部挡死，且 `no-cors`/canvas 都绕不过去

## 31. 域名白名单要让未知项可见，不要静默跳过
**规则**: 涉及外部资源抓取的白名单，遇到不在名单里的域名要逐 host 计数、写进 meta 并在界面显示出来，让用户能反馈；样本量小的时候尤其不能假设名单已经完整
**反例**: 小号只有 40 张图，不足以证明 23281 条的大号里没有其它历史图床；静默跳过会让用户以为备份完整，实际缺图且无从发现

## 32. 设置页 async 初始化失败必须可见
**规则**: `App.init()` 这类"失败即白屏"的顶层 async 初始化，异常要有 catch 并渲染出错误文本；判断白屏成因时先看是否卡在 `isReady` 之前，而不是先怀疑样式或资源加载
**反例**: 一个 `...undefined` 就让整页空白且无任何提示 → 只能靠读源码定位，用户侧完全无法自助排查

## 33. Worktree 路径必须遵循项目约定
**规则**: 创建或迁移 worktree 前先读取项目的 `WORKTREES.md`，统一放到仓库内的 `worktrees/`，并用 `git worktree list` 验证注册路径；不要默认使用 `projects/.worktrees/`
**反例**: worktree 虽然能正常工作，但散落在项目外层，导致归属不清、项目文档与实际路径不一致
